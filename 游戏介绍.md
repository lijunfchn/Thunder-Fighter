# 雷霆战机 · Thunder Fighter

> 一款用 **单文件 C++ / Win32 API 从零手写**的竖版飞行射击（STG）游戏。
> 不含任何游戏引擎、第三方库或外部素材 —— 图形全部由 GDI 几何图元绘制，音效与 BGM 全部由代码实时合成。

---

## 一、基本信息

| 项目 | 内容 |
| --- | --- |
| 游戏名称 | 雷霆战机（Thunder Fighter） |
| 版本 | v2.0 |
| 类型 | 竖版弹幕飞行射击 / STG |
| 内部渲染分辨率 | 480 × 720（9:16 竖屏），窗口可自由缩放，等比缩放 + 黑边自适应 |
| 开发语言 | C++（单文件 `main.cpp`，约 3180 行） |
| 技术栈 | Win32 API + GDI + `winmm`（`waveOut` 音频输出） |
| 依赖 | 仅 Windows 系统库：`user32` / `gdi32` / `winmm`，**零第三方依赖** |
| 逻辑帧率 | 固定 60 FPS 时间步长（`QueryPerformanceCounter` 高精度计时） |
| 可执行文件 | `ThunderFighter.exe`（约 2.4 MB，静态链接） |

### 设计亮点

- **全部程序化生成（Procedural）**：没有一张图片、没有一个音频文件。飞机、敌机、Boss、粒子、辉光、星云全部用 GDI 多边形/椭圆/像素绘制；16 种音效与整段循环 BGM 由代码用正弦波、方波、锯齿波、三角波和噪声合成。
- **从零实现的软件音频混音器**：自建 24 声道发声体池，支持扫频、包络、颤音、延迟回声与左右声像定位，多音效叠加不互相打断。
- **打击感（Game Feel）系统**：屏幕震动、白屏闪烁、慢动作、受击闪白、伤害飘字、辉光叠加，一应俱全。
- **零每帧资源泄漏**：字体与辉光贴图按需缓存复用，背景预渲染，双缓冲绘制杜绝闪烁。

---

## 二、编译与运行

### 一键编译

直接双击 `compile.bat`，脚本会依次检测 **MinGW g++** 和 **MSVC cl**，自动选择可用编译器完成编译并启动游戏。

### 手动编译

```bash
# MinGW (g++)
g++ -o ThunderFighter.exe main.cpp -lgdi32 -lwinmm -static -O2

# MSVC (cl)
cl main.cpp user32.lib gdi32.lib winmm.lib /Fe:ThunderFighter.exe /O2
```

编译后直接运行 `ThunderFighter.exe` 即可，无安装过程。

---

## 三、操作方式

| 按键 | 功能 |
| --- | --- |
| `↑` `↓` `←` `→` | 移动飞机（带加速度与惯性，非匀速直移） |
| `SPACE` | 按住射击；按住期间火力持续累积，**蓄满 75 帧（1.25 秒）后**改用蓄力状态（此时停止普通射击），松手释放蓄力激光 |
| `B` | 使用炸弹（清屏 + 全体敌人 10 点伤害） |
| `ESC` | 游戏中→暂停；暂停中→继续；结算界面→返回主菜单 |
| `R` | 暂停界面中重新开始 |
| `ENTER` | 主菜单/结算界面开始游戏；暂停界面继续 |
| `←` `→`（主菜单） | 切换游戏模式 |
| `↑` `↓`（主菜单） | 切换机体（仅限已解锁机体） |

---

## 四、游戏模式

主菜单可用 `←` `→` 在三种模式间切换：

### 1. 剧情模式（Story）

标准闯关流程。每关需要击落指定数量的敌机（`12 + 关卡数 × 2` 架 —— 第 1 关 14 架、第 2 关 16 架……），清空后进入下一关。

- **每 5 关为一个 Boss 关**（第 5、10、15、20…… 关），该关只出现一架 Boss。
- **关卡数无上限**，可持续无限闯关。
- 清空条件满足后需等待 **2 秒（120 帧）**结算，随后进入下一关。
- 过关奖励：`500 + 关卡数 × 50` 分（第 1 关 +550、第 2 关 +600……），并播放过关音效。
- 若整关**未受任何伤害**通过，解锁成就「无伤通关」。
- 每关开场显示剧情横幅（如「第 3 关 - 敌军来袭」「第 5 关 BOSS 来袭!」）。

### 2. 生存模式（Survival）

无尽模式，敌机无限生成（内部上限占位 999999），比拼**存活时间**。

- 难度增速更快：每 **450 帧（7.5 秒）** 难度 +1（其他模式为每 600 帧即 10 秒）。
- **永不出现 Boss**，只无限刷普通敌机（大型敌机仍会作为精英单位出现）。
- 关卡数恒为 1，不推进关卡，纯粹比拼存活时间。
- HUD 与结算界面显示「生存: N 秒 / 击杀: N」。

### 3. Boss Rush

车轮战模式，Boss 一只接一只出现，永不间断。

- Boss 血量更高：`20 + 序号 × 10`。
- 击破奖励：`800 + 已击败数 × 100` 分，随后立刻刷新下一只。
- 弹幕模式按序号循环轮换（突击 → 弹幕 → 召唤 → 多阶段 → 散射）。
- 结算界面显示「击败 Boss: N 个」。

---

## 五、机体系统

三架机体拥有不同初始属性与配色，后两架需通过**成就**解锁（解锁状态跨局永久保存）。

| 机体 | 生命 | 炸弹 | 初始火力 | 速度倍率 | 配色 | 解锁条件 |
| --- | :-: | :-: | :-: | :-: | --- | --- |
| **标准战机** | 3 | 2 | ★ | 1.00× | 天蓝 | 默认拥有 |
| **重装战机** | 4 | 3 | ★ | 0.85× | 橙红 | 成就「初出茅庐」 |
| **疾风战机** | 2 | 1 | ★★ | 1.25× | 荧光绿 | 成就「弹幕舞者」 |

未解锁的机体会以 `[ ??? ] 未解锁（达成成就解锁）` 灰字占位，切换时会自动跳过。

---

## 六、武器系统

游戏中通过 **W 道具（更换武器）** 在三种特殊武器间循环切换；丢弃后回到默认机枪。

### 1. 机枪（默认，`WEAPON_MG`）

单发伤害 1，弹速随火力等级提升（`8 + 等级 × 0.3`）。全部子弹**平行上射（无角度扩散）**，仅发射点位置不同。

| 火力等级 | 弹数 | 弹型 | 射击间隔 |
| :-: | --- | --- | :-: |
| ★ | 1 发居中 | 标准 | 12 帧 |
| ★★ | 2 发并列 | 标准 | 11 帧 |
| ★★★ | 3 发（中部 1 标准 + 两侧 2 细弹） | 标准 + 4×12 | 10 帧 |
| ★★★★ | **5 发**（中部 1 + 两侧 2 + 更外侧 2 细弹） | 标准 + 4×14 + 4×10 | 8 帧 |

### 2. 散射（`WEAPON_SCATTER`）

**5 方向扇形弹幕**，弹数 `3 + 火力等级`（3～6 发），单发伤害 1，射击间隔 `13 - 等级` 帧。清杂兵效率极高。

### 3. 追踪导弹（`WEAPON_HOMING`）

**自动锁定最近的敌人**并转向追击。弹数 `1 + 火力等级`（1～4 发），**单发伤害 2**，射击间隔 `22 - 等级 × 2` 帧。适合对付高速移动的敌机。

### 4. 激光（`WEAPON_LASER`）

**穿透型持续伤害光束**，可贯穿多个敌人。弹数随火力等级 `1 + 等级 / 2`（★/★★ 为 1 道，★★★/★★★★ 为 2 道，间距 16 px），单发伤害 1，弹速 12（全场最快），射击间隔 `8 - 等级 / 2` → **8 / 8 / 7 / 7 帧**。
被激光命中的敌人有 **8 帧个体冷却**，避免同一敌人每帧被反复扣血。

### 蓄力激光（Charge Laser）

- 按住 `SPACE` 时蓄力值持续积累：**120 帧（2 秒）** 为上限，**达到 75 帧** 即进入「蓄力完成」状态（播放专属提示音，机头亮起辉光并出现预示光束），此后**停止普通射击**。
- 关键细节：**75 帧之前的蓄力期间普通武器照常开火**，只有跨过 75 帧门槛后才会中断常规射击。
- 在 ≥75 帧时**松开空格**即释放：一道 **14 × 240 像素、伤害 5 的穿透重激光**，附带爆炸粒子、环形冲击波、屏幕震动与音效。
- 释放后蓄力值无条件归零；若未达 75 帧阈值就松手，则不会释放激光（仅当作普通射击）。
- 75 帧与 120 帧释放的效果**完全相同**，蓄满到 120 帧只是蓄力条继续累积（显示被截断），不会额外增强。

---

## 七、道具系统

击毁敌机有概率掉落道具，下落速度为 2 px/帧，超出屏幕自动消失。

| 道具 | 图标 | 颜色 | 掉落权重 | 效果 |
| --- | :-: | --- | :-: | --- |
| **火力提升** | `P` | 金黄 | 40% | 火力等级 +1（上限 ★★★★） |
| **护盾** | `S` | 青色 | 15% | 获得护盾，持续 **5 秒**；被击中时先破盾，不受伤害 |
| **炸弹** | `B` | 红色 | 12% | 增加 1 枚炸弹**并立刻引爆一次**（拾取即触发全屏清弹，等于当场的解围手段） |
| **更换武器** | `W` | 淡蓝 | 18% | 切换武器：机枪 → 散射 → 追踪导弹 → 激光 → 散射（循环，**不会再回到机枪**） |
| **轨道盾** | `O` | 青绿 | 15% | 3 枚环绕弹围绕机体旋转 **8 秒**，可击落碰到的敌弹（每个 +5 分） |

**敌机掉落概率**：小型机 6%、中型机 22%、Boss **70%**、其他特殊机 14%。道具池上限 6 个，同屏多余的掉落会被丢弃。

---

## 八、敌机图鉴

| 类型 | 形态 | 血量 | 移动/攻击特征 | 击毁得分 |
| --- | --- | --- | --- | :-: |
| **小型敌机** | 红色菱形 | 1 | 移速为基准的 **1.5 倍**；三种摆动模式，单发直射弹 | **100** |
| **中型敌机** | 品红六边形 | `3 + 难度/3` | 直射弹，1/5 概率追加两发侧向弹；显示血条；射击间隔 45～85 帧 | **300** |
| **自爆机** | 橙色倒三角 + 危险辉光 | 1 | **不射击**，以 `4.2 + 难度 × 0.15` 的速度直冲玩家；**贴近至 36 像素内自爆**（无敌帧期间自爆不造成伤害） | **200** |
| **护盾机** | 方形机身 + 前方能量盾 | 3 + **盾 3** | 必须先击破 3 点能量盾才能伤及本体；发射瞄准玩家的直射弹（速度 3.6） | **300** |
| **召唤机** | 紫色六边形 + 光环 | `5 + 难度/2` | **不射击**，缓降摆动；每 **150 帧召唤 2 架**小型机 | **300** |
| **大型敌机（Boss 级）** | 紫色八边形 + 底光 | `8 + 难度/2` | 关卡中随机出现的精英单位，移速为基准的 0.5 倍，使用散射弹幕 | **1000** |
| **Boss** | 八边形 + 狂暴核心 | `20 + 关卡 × 6` | 五种弹幕模式，悬停巡逻，半血狂暴 | **1000** |

### 编队与特殊机制

- **V 字形编队**：难度 ≥ 1 且非 Boss 关时，生成敌机有 15% 概率改为一次生成 **5 架 V 字形编队**（编队机不射击，整体摆动下降，纯走位压迫）。
- **特殊敌机**：难度 ≥ 3 时，20% 概率生成护盾机或召唤机（各半）。
- **自爆机**：难度 ≥ 2 时，14% 概率将小型/中型敌机替换为自爆机（Boss 关不出现）。
- **敌机移速**：基准速度为 `1.0 + 难度 × 0.1`，再乘类型系数（小型 1.5×、Boss 级 0.5×、护盾机 1.1×）。
- **半血狂暴**：Boss 血量降到 50% 以下时——
  - 射击间隔**减半**；
  - 每轮额外发射 5 发扇形弹；
  - 机体变红，中心出现脉动能量核心，辉光转红。

---

## 九、Boss 弹幕模式

Boss 使用 5 种弹幕模式，剧情模式中按关卡数分配，Boss Rush 中按序号循环：

| 模式 | 名称 | 弹幕行为 |
| :-: | --- | --- |
| **0** | 散射型 | 三向散弹 + 1/3 概率追加一发高速重弹 |
| **1** | 弹幕型 | **双螺旋弹幕**，角度持续递增，形成旋转弹雨 |
| **2** | 召唤型 | 瞄准玩家的大弹 + 三向扇形 + 1/3 概率 7 发横向扫射弹排；**悬停至 y ≥ 100 后每 200 帧召唤 1 架小型机** |
| **3** | 多阶段 | 第一形态为散射，**半血时切换第二形态**（红色冲击波 + 白闪 + 震屏）改为**螺旋弹幕**，并从悬停转为上下浮动 |
| **4** | 突击型 | 高速瞄准弹（全场最快）+ 左右两侧斜向弹；机体会**持续追踪玩家的横向位置** |

**剧情模式模式分配**（按关卡数自上而下优先匹配）：第 20 关多阶段、第 15 关召唤型、第 10 关弹幕型，其余 Boss 关（5、25、35……）为突击型。注意匹配优先级为 `%20` → `%15` → `%10`，因此第 30 关取**召唤型**（`%15` 先命中），而非弹幕型。

**Boss Rush 模式分配**：按已击败序号 `% 5` 循环 —— 突击 → 弹幕 → 召唤 → 多阶段 → 散射。

Boss 出现前会播放 klaxon 警报音、显示闪烁的 `⚠ WARNING ⚠` 警示横幅，并伴随屏幕震动。

---

## 十、得分与连击

### 得分规则

| 来源 | 分数 |
| --- | --- |
| 击毁小型 / 自爆 / 中型 / 护盾 / 召唤 / Boss 级 | 100 / 200 / 300 / 300 / 300 / 1000 |
| 过关奖励 | `500 + 关卡数 × 50` |
| Boss Rush 击破奖励 | `800 + 已击败数 × 100` |
| 轨道盾拦截敌弹 | 每发 +5 |

### 连击（Combo）机制

- 每次由**子弹/激光**击毁敌机时**连击 +1**；连击 > 1 时，单次得分乘以 **`1 + 连击 × 0.1`** 的倍率（10 连击 = 2.0 倍，50 连击 = 6.0 倍，无上限）。
- 连击达到 **10 / 20 / 50 / 100** 时触发里程碑：额外奖励 `连击数 × 10` 分，全屏播放粉色冲击波、白闪与专属音效。
- **连击没有时间衰减**，但**受到真实伤害时立即归零**（被护盾挡下则不影响连击）。
- **炸弹击杀不计连击**（仍正常计分与掉落道具）。

最高分独立保存在 `highscore.txt`，主菜单与结算界面均会显示。

---

## 十一、成就与解锁

成就跨局永久保存（`save.dat`），达成时全屏弹出金色辉光横幅、号角音效与冲击波。

| 成就 | 达成条件 | 奖励 |
| --- | --- | --- |
| **初出茅庐** | 累计击毁 **100** 架敌机（跨局累计） | 解锁「重装战机」 |
| **无伤通关** | 剧情模式下某一关**全程未受伤** | — |
| **弹幕舞者** | 屏幕上同时存在 **50 发以上敌弹**并**坚持 10 秒**（600 帧） | 解锁「疾风战机」 |

---

## 十二、伤害与生命

- 玩家初始生命由机体决定（2～4），主菜单与 HUD 右上角以小红机图标显示。
- 被敌弹或敌机撞到即受伤：

| 状态 | 结算 |
| --- | --- |
| 持护盾 | 仅破盾，不扣生命、不进入无敌 |
| 无护盾 | 生命 −1，**火力等级 −1**，连击归零，获得 **1.5 秒（90 帧）无敌**（半透明呼吸闪烁） |

- 生命归零后进入约 **2 秒**的死亡演出（1 秒大爆炸 + 60 帧倒计时），随后进入结算界面。
- 无敌条期间**自爆机贴身自爆也不会造成伤害**，可放心穿越。
- 结算界面显示最终得分、模式专属成绩（到达关卡 / 生存时间 / 击败 Boss 数）与最高分。

---

## 十三、炸弹（Bomb）

按 `B` 使用（初始 2～3 枚，拾取 `B` 道具补充）：

- 清除屏幕上**所有敌方子弹**；
- 对**所有场上敌人造成 10 点伤害**（足以秒杀中小型敌机），致命者依次结算击毁；**该伤害直接扣除本体血量，会无视护盾机的能量盾**；
- 全屏 4 道随机冲击波 + 40 组白热粒子爆发；
- 屏幕震动（强度 12 / 30 帧）、白屏闪烁、**慢动作 35 帧**（时间流速降至 45%）；
- 播放低频噪声爆炸音效。
- 由炸弹造成的击毁**不计入连击**（仍正常计分与掉落道具）。

---

## 十四、音频设计（纯代码合成）

### 实时音效混音器

- 采样率 **44100 Hz 立体声**，输出缓冲区 1024 帧 × 3 块循环（约 23 ms 延迟），通过 `waveOut` + 回调持续填充。
- **24 个并发发声体（voice）**，声部占满时新音效被丢弃而非打断已有声音。
- 每个音效由最多 **5 层** 叠加构成，每层可指定波形、起止频率（指数扫频）、时长、音量、起音、衰减指数与延迟。
- 支持**左右声像定位**：采用等功率声像（`gl = cos θ`、`gr = sin θ`），敌机爆炸音效按其屏幕横坐标定位声场（`PlaySfxPan`），散射与追踪导弹的发射音也分别偏左/偏右。
- 混音总线做 0.9 增益并逐样点钳位到 16-bit 范围，避免削波破音。

**波形编码**：正弦（0）、方波（1）、锯齿（2）、三角（3）、噪声（4）。

**16 种音效**：射击、激光、导弹发射、小爆炸、大爆炸、受伤、道具、炸弹、Boss 警报、蓄力完成、蓄力激光释放、连击、成就、护盾击破、过关。

### BGM

启动时**预合成一段 8 小节循环 BGM** 并循环播放：

- **132 BPM，A 小调**，8 小节（约 14.5 秒循环）；
- 编制：底鼓（正弦下扫 + 噪声起音）、军鼓（噪声 + 短音身）、踩镲、三角波贝斯（八分音符律动）、方波琶音（十六分音符）、正弦主旋律（带颤音与 3/4 拍延迟回声）；
- 和声进行在 A 小调四个和弦间循环；
- 游戏中按 `ESC` **暂停时会停掉 BGM**，继续游戏或返回主菜单时自动恢复；**恢复时从上次位置续播**而非从头开始（BGM 游标不会被重置）。
- 游戏结束（进入结算界面）时同样停止 BGM。
- 暂停/菜单/结算状态下星空仍以 0.4 倍速缓慢流动，画面不死板。

---

## 十五、视觉与打击感

### 渲染架构

- **全程序化绘制**：飞机为六点轮廓多边形 + 引擎双层火焰（长度随机抖动）；小/中型/自爆/护盾/召唤/Boss 各有专属几何造型与配色；敌弹统一为品红椭圆 + 白芯（高辨识度，便于读弹）。
- **加法辉光**：预生成 64×64 径向衰减亮度图并按颜色缓存，用 `SRCPAINT` 做加法混合，实现标题、机体、Boss、道具、子弹的光晕。
- **预渲染背景**：垂直渐变深空（深蓝 → 近黑）+ 4 处多层同心椭圆叠加的柔和星云 + 远景星点，仅构建一次，每帧一次 `BitBlt`。
- **三层视差星空**：90 颗星分远景/中景/近景三层，速度 1～6 px 递增，带正弦闪烁，近景星为 2×2 像素块。
- **双缓冲 + 最近邻缩放**：所有绘制先入内存位图，再按保持宽高比的方式 `StretchBlt` 到窗口（`COLORONCOLOR` 避免抖动与闪烁）；`WM_ERASEBKGND` 直接返回 1。
- **轻量半透明**：暂停/结算界面的暗化遮罩用 50% 斜纹画刷模拟（GDI 无 alpha 通道的经典替代方案）。

### 打击感清单

| 效果 | 实现 |
| --- | --- |
| 屏幕震动 | 游戏对象整体偏移，HUD 不震；强度 5～14、持续 10～40 帧 |
| 白屏闪烁 | 炸弹 12 帧、Boss 击毁 10 帧，随时间柔和衰减 |
| 慢动作 | Boss 击毁 40 帧、炸弹 35 帧，时间步长缩放至 0.45 倍 |
| 受击闪白 | 敌机被命中时整体变白 3 帧 |
| 伤害飘字 | 高伤害武器命中时飘出 `-N`；得分、连击、拾取道具均有飘字 |
| 环形冲击波 | 爆炸时扩散的圆环粒子（含双层大爆炸） |
| 血条变色 | 绿 → 黄 → 红三段，Boss 半血变红并出现狂暴核心 |
| 视差 + 辉光 + 粒子 | 220 个粒子池循环复用，爆炸按机型分级（14～48 粒） |

### 界面动画

- 标题双层描边 + 双辉光叠加，装饰线两端带亮点；
- 提示文字使用**正弦呼吸脉冲**（0.55～1.0）替代生硬的开关闪烁；
- 蓄力条随蓄力进度填充，满蓄力时机头辉光脉动 + 前方预示光束闪烁。

---

## 十六、存档文件

| 文件 | 内容 | 格式示例 |
| --- | --- | --- |
| `highscore.txt` | 历史最高分（纯数字） | `62767` |
| `save.dat` | 成就位掩码、机体解锁位掩码、累计击毁数 | `1 3 100` |

`save.dat` 中 `1 3 100` 表示：成就位掩码 `1`（仅达成「初出茅庐」，位 0）、机体解锁掩码 `3`（标准战机 + 重装战机）、累计击毁 100 架。

> 注：成就「初出茅庐」达成时会自动解锁重装战机，「弹幕舞者」达成时自动解锁疾风战机。

---

## 十七、源码结构（`main.cpp`）

| 行号区间 | 模块 |
| --- | --- |
| 17–160 | 游戏常量（分辨率、尺寸上限、武器/模式/机体/成就编号）与数据结构（Bullet / Enemy / Particle / PowerUp / FloatingText） |
| 162–325 | 音频合成工具：波形烘焙（`BakeNote` / `BakeNoise` / `BakeKick` / `BakeSnare`）、音效层配方表 |
| 327–683 | 游戏主类成员、初始化、重置、最高分与存档读写、机体定义、成就系统 |
| 684–905 | 音频系统：BGM 预合成、软件混音器、`waveOut` 回调、声像定位音效 |
| 906–941 | 固定时间步长主循环（QPC + 累加器，1/60 秒步进，慢动作缩放） |
| 943–1070 | 视觉辅助：颜色缩放、多边形/椭圆绘制、字体与辉光缓存、背景预渲染 |
| 1072–1204 | 打击感辅助：震动、闪烁、慢动作、飘字、环形冲击波、击毁结算 |
| 1205–1442 | 关卡流程、Boss 生成、编队、召唤小兵、敌机生成、道具生成 |
| 1444–1598 | 玩家射击（四种武器）与蓄力激光释放 |
| 1600–1698 | 敌人射击（按机型与 Boss 弹幕模式） |
| 1700–2254 | 碰撞检测与更新逻辑（移动、碰撞、道具、粒子、连击、成就、结束判定、炸弹） |
| 2256–3014 | 渲染（背景、菜单、游戏画面、HUD、暂停、结算） |
| 3016–3081 | 按键处理 |
| 3083–3183 | 窗口过程与 `WinMain` 入口 |

---

## 十八、快速上手建议

1. **先玩剧情模式「标准战机」**，熟悉惯性移动手感——飞机不是瞬时跟随，松开方向键会滑行一小段。
2. **主武器点按即可**：一直长按空格会进入蓄力——蓄力超过 1.25 秒后普通射击就停了，等到你松手才发射激光，中间那段时间的输出反而变低。想放蓄力激光就果断松手，不想放就**点按**。
3. **优先吃 `P`（火力）**：火力等级直接决定弹幕密度，也是受伤时唯一会掉的强化，损失很大。
4. **`W`（换武器）是战术资源**：清杂兵用「散射」，打 Boss 用「激光」或「追踪导弹」；注意循环一旦切走就**回不到机枪**。
5. **炸弹不要留到死**：它能清掉满屏弹幕并造成 10 点全体伤害，是保命也是解围的关键；拾取 `B` 道具还会当场自动引爆一次。
6. **想解锁疾风战机**：主动钻进弹幕里——让屏幕上同时存在 50 发以上敌弹并**连续**坚持 10 秒（中途掉到 50 发以下计时会清零重来）。
7. **想解锁重装战机**：多刷关卡累计击毁 100 架敌机（跨局累计，很快）。
8. **看清品红弹**：所有敌方子弹都是统一的品红椭圆加白芯——这个配色就是为了在混乱的满屏弹幕里第一时间读出危险。

---

*雷霆战机 v2.0 — C++ Win32 Game · 单文件实现，零第三方依赖*
